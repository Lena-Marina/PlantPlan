import { User } from './user';

/*
describe('User', () => {
  it('should create an instance', () => {
    expect(new User(randomUUID(), 'John Doe', 'john.doe@example.com', true, "EN", 0, new)).toBeTruthy();
  });
});
*/
