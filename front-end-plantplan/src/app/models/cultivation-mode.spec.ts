import { CultivationMode } from './cultivation-mode';
import { CultivationStep } from './cultivation-step';

/*
describe('CultivationMode', () => {
  it('should create an instance', () => {
    expect(new CultivationMode("Test Cultivation Mode", new CultivationStep("Test Cultivation Step"))).toBeTruthy();
  });
});
*/
